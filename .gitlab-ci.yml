image:
  name: hauer/dojo:yamllint-1.23
  entrypoint: ['bash', '-c', 'chown dojo:dojo -R /builds/jan/dojo && rmdir /dojo/work && ln -s /builds/jan/dojo /dojo/work && /usr/bin/entrypoint.sh']

test gradle:
  stage: test
  script:
    - pwd
    - ls -la
    - cd /dojo/work
    - ls -la
    - echo $GIT_CLONE_PATH