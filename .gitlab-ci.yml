image:
  name: hauer/dojo:yamllint-1.23
  entrypoint: ['bash', '-c', 'chown dojo:dojo -R /builds/jan/dojo && exec su dojo -c bash']

test gradle:
  stage: test
  script:
    - pwd
    - ls -la
    - cd /dojo/work
    - ls -la