image:
  name: hauer/dojo:jre-15

variables:
  DOJO_WORK_INNER: $CI_PROJECT_DIR

test gradle:
  stage: test
  script:
    - pwd
    - ls -la
    - echo $dojo_home
    - ls -la $dojo_home
    - echo $dojo_work
    - ls -la $dojo_work
    - java --version